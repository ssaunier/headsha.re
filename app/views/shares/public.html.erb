<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <style type="text/css">
    header { background-color: #111; }
    header a { color: #eee; }

    body {
      margin: 0;
      padding: 0;
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    }
    header {
      height: 16px;
      width: 100%;
      text-align: center;
      padding: 8px 0;
      font-size: 12px;
      line-height: 16px;
    }
    header a {
      text-decoration: none;
      width: 100%;
      display: inline-block;
    }
    header a:hover {
      text-decoration: underline;
    }
    .container {
      width: 100%;
      position: fixed;
      top: 32px;
      bottom: 0;
    }
    .container iframe {
      width: 100%;
      height: 100%;
    }
  </style>
</head>
<body>
  <header id="header">
    <a href="<%= @share.header_url %>">
      <%= @share.header_content %>
    </a>
  </header>
  <article class="container">
    <iframe src="<%= @share.content_url %>" frameborder="0"></iframe>
  </article>
</body>
</html>